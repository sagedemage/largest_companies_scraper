[project]
name = "largest_companies_scraper"
version = "0.1.0"
description = ""
authors = [
    {name = "Salmaan Saeed"}
]
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "beautifulsoup4 (>=4.14.3,<5.0.0)",
    "lxml (>=6.1.1,<7.0.0)",
    "pandas (>=3.0.3,<4.0.0)",
    "certifi (>=2026.5.20,<2027.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
[dependency-groups]
dev = [
    "deptry (>=0.25.1,<0.26.0)"
]
